-- Match an IPv4 address: four dot-separated groups of digits.
-- Does not validate that each octet is <= 255.
--
-- Usage:
--   echo -n "192.168.1.1" | pars examples/ipv4.pars

use "std/abnf";

ipv4 = octet '.' octet '.' octet '.' octet
  where
    octet = DIGIT+
  end
